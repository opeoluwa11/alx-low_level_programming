#include "main.h"
/**
 * print_alphabet - and all lowercase
 *
 * Return: Always 0
 */
void print_alphabet(void)
{
	char alphabet;

	for (alphabet = 'a'; calphabet <= 'z'; alphabet++)
	{
		_putchar(c);
	}
_putchar('\n');

return (0);
}
